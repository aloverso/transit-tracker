type Query {
    modes: [TransitMode]
}

type Mutation {
    addMode(name: String): TransitMode
    incrementMode(id: Int): TransitMode
}

type TransitMode {
    name: String
    counter: Int
    id: Int
}

