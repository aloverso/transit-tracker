type Query {
    modes: [TransitMode]
}

type Mutation {
    addMode(name: String): TransitMode
    incrementMode(name: String): TransitMode
}

type TransitMode {
    name: String
    counter: Int
}

